import Controller from "./Controller";

const app = new Controller("#slider", {
  // min: 1000,
  // max: 3000,
  tooltip: true,
  // from: 250,
  // to: 500,
  range: true,
  configPanel: true,
  // to: 2000,
});
