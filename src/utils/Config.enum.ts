enum TogglesEnum {
  VERTICAL = "vertical",
  RANGE = "range",
  RULERS = "rulers",
  PROGRESS = "progress",
  TOOLTIP = "tooltip",
}

enum ControlsEnum {
  MIN = "min",
  MAX = "max",
  STEP = "step",
  FROM = "from",
  TO = "to",
}

export { TogglesEnum, ControlsEnum };
