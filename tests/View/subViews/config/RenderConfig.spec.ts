import $ from "jquery";
import { RenderConfig, CreateConfig } from "../../../../src/View/subViews";
import ModelOption from "../../../../src/utils/ModelOption";
import Model from "../../../../src/Model/index";

describe("Render Config test", () => {
  // const body: JQuery = $("body");
  // const config: CreateConfig;
  // const options: ModelOption = new Model({ configPanel: true });
  // const renderConfig = new RenderConfig(options, config, body);
  // test("Should", () => {});
});
